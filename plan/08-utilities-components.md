# Utilities And Components

Goal:

- Add reusable Tailwind-side utilities for repeated visual patterns.

Possible patterns:

- notebook board shell
- taped card corners
- scribble tooltip

Notes:

- In Tailwind v4, this can often live in CSS with `@utility` or `@layer`.
- Not everything has to go into config directly.
